define(['./config/module'], function (module) {
    'use strict';

    module.controller('dashboard/main', ['$scope', function ($scope) {

        /** scope manipulation here **/
        console.info('Controller dashboard/main loaded');
    }])
});